import type { StyleConfig } from './minimal.js';

export const modernStyle: StyleConfig = {
  backgroundColor: '#4A90E2', // Gradient will be applied
  deviceFramePadding: 80,
  textPosition: 'top',
  textColor: '#FFFFFF',
  titleSize: 52,
  subtitleSize: 32,
  textPadding: 50,
};
