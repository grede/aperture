import type { StyleConfig } from './minimal.js';

export const darkStyle: StyleConfig = {
  backgroundColor: '#1A1A1A',
  deviceFramePadding: 70,
  textPosition: 'bottom',
  textColor: '#FFFFFF',
  titleSize: 50,
  subtitleSize: 25,
  textPadding: 45,
};
