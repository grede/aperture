import type { StyleConfig } from './minimal.js';

export const gradientStyle: StyleConfig = {
  backgroundColor: '#FF6B6B', // Gradient will be applied
  deviceFramePadding: 100,
  textPosition: 'top',
  textColor: '#FFFFFF',
  titleSize: 56,
  subtitleSize: 28,
  textPadding: 60,
};
