import type { StyleConfig } from './minimal.js';

export const playfulStyle: StyleConfig = {
  backgroundColor: '#FFD93D',
  deviceFramePadding: 90,
  textPosition: 'top',
  textColor: '#2D3436',
  titleSize: 54,
  subtitleSize: 27,
  textPadding: 55,
};
